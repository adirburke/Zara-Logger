import XCTest
@testable import Zara_LogService

final class Zara_LogServiceTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Zara_LogService().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
